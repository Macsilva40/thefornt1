import { makeStyles } from "@material-ui/core";

const styles = makeStyles(() => ({
  icon: {
    fontSize: 30,
    color: "rgb(63, 81, 181)",
  },

  linha: {
    padding: 30,
  },
}));

export default styles;
